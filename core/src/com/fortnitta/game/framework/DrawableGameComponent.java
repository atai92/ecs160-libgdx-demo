package com.fortnitta.game.framework;


/** A game component that is notified when it needs to draw itself.
 *
 * This could be a Tile, a Map, a Character, a ParticleEffect, a Menu.
 */
public abstract class DrawableGameComponent extends GameComponent implements Drawable {
}
